
import java.time.LocalDate;

/**
 *
 * @author POO
 * 
 */
public class ListOfElements {
    /**
     * 
     * Pretende-se efetuar a construção da classe principal da solução (ListOfElements). Esta classe será responsável por armazenar, organizar e ordenar as tarefas. O principal requisito da classe passa por aceitar apenas elementos que implementem a interface Orderable.

Crie a classe ListOfElements de forma a receber um elemento genérico que implemente a interface Orderable.

Assegure que essa classe (ListOfElements) estende ArrayList para armazenar os elementos do tipo genérico definido no ponto anterior.

Adicione à classe a seguinte constante pública:

HEADER - String que representa o cabeçalho a incluir antes de listar os elementos da lista.
Adicione um construtor à classe. Este construtor recebe, por parâmetro, o valor a atribuir ao atributo HEADER
     */

     private String HEADER;

     public ListOfElements
}
